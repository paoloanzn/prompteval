Generate an evaluation dataset for testing prompts that classify a short Python
snippet for a specific security vulnerability category.

Output format:
<json_block>
[
  {"task": "Classify the following Python snippet. Decide whether it contains the\nvulnerability category listed, and if so, the line where it is introduced.\n\nCategory under test: <sql_injection|path_traversal|command_injection|unsafe_deserialization|hardcoded_secret|ssrf>\n\n```python\n<5-25 line snippet, lines numbered starting at 1>\n```\n\nFormat:\n  VULNERABLE: <yes|no>\n  LINE: <integer line number, or - if no>\n  EVIDENCE: <one short clause naming the sink and the tainted source, or - if no>"},
  {"gold": "Correct classification"}
]
</json_block>

Constraints on tasks:
* Roughly half positive, half negative. Negatives must include lookalikes:
  parameterized queries that resemble concatenation, os.path.join on a
  constant, subprocess with shell=False and a list argv, pickle.loads on a
  bytes literal defined in-source, secrets loaded from os.environ, urllib
  requests to a constant URL.
* Positives must have an unambiguous taint path from an external source
  (request arg, sys.argv, file read, env-derived URL, deserialized payload)
  to the dangerous sink, with no sanitization or allowlist in between.
* Each snippet is self-contained: all imports shown, no helper functions
  referenced outside the snippet, so the verdict is derivable from the task
  alone.

Generate 20 objects.